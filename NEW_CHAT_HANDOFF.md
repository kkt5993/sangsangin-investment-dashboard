# 새 채팅 시작 인계

## 시작 기준

- 저장소는 `main`이며, 이 문서 작성 시 원격과 같은 최신 커밋은 `2c4ffe9` (`Refresh validated research snapshots`)이다. 새 채팅에서 먼저 `git status --short`와 `git log -1 --oneline`으로 재확인한다.
- 가장 최근 승인 자동 실행은 run `20260913T230054Z`, 가격 기준일 `2026-09-13`, raw 빈티지 `20260913T230054Z`다. `docs/data/refresh.json`은 `validated`를 기록하며 1,168개를 시도해 1,167개 변경, 실패 1개, 신규 가격 요청 25개·보류 6개를 기록한다. 이는 로컬 검증 결과이며 운영 게시 여부는 실행 원장과 운영 `refresh.json`/`status.json`을 함께 대조해야 한다.
- 직전 안정 게시 기준은 run `20260913T123639Z`, vintage `20260913T112310Z`, 생성 결과 커밋 `f4bb326`이다. 이 기준과 새 run을 혼동하지 않는다.

## 반드시 읽을 문서

1. `AGENTS.md` — 데이터 보관, 수집·배포, 검증과 공개 범위 규칙.
2. `HANDOFF.md` — 구현·검증·원자료 빈티지의 상세 시간순 기록.
3. `research/WORK_INVENTORY.md` — Markdown에서 추출한 현재 정본 후보와 역사 증거. 체크박스는 완료 판정이 아니며 코드·원자료·권한·실행 원장으로 재검증한다.
4. `research/REFERENCE_PARITY.md`, `research/ROADMAP.md`, 관련 `*_CONTRACT.md` — 탭별 실제 범위와 남은 계약.
5. `docs/data/status.json`, `docs/data/refresh.json` — 현재 공개 계산과 가장 최근 실행의 기계 판독 상태.

## 이번까지 완료한 핵심 변경

- 관계 연구 표기 159개 중 153개를 149개 종목으로 식별해 명시 등록부에 넣고, 신규 가격은 실행당 25개씩 증분 수집하도록 했다. 해외 상장/주식 클래스가 확인되지 않은 6개는 임의 매핑하지 않는다.
- SEC 13F는 새 공시가 정상 수집될 때 최대 4개 보고기간·정정·인접 분기 보고금액 차이를 누적하도록 구현했다. Form4/A는 원공시가 하나로 확인되는 경우만 대체한다.
- 임상 표본의 비모집→`RECRUITING` 전환 알림, 문서 page ID/Wikidata 정체성 확인, 기업 뉴스의 공식 KR/US 유니버스 확장, 한국 거시 발표 달력 연결을 구현했다.
- 재무 raw 캐시는 945개 중 930개가 정상이며 15개는 손익 항목 부재 또는 재무 통화 미확인으로 보존한다. 수치를 추정해 정상 자료로 바꾸지 않는다.
- `scripts/write_work_inventory.py`가 섹션 제목을 독립 작업으로 세지 않도록 보완했고 회귀 테스트를 추가했다. 전체 Python 399개(94.933초), 정적 공개 검사를 통과한 근거는 `HANDOFF.md`에 있다.

## 자동 실행과 배포

- Codex heartbeat ‘상상인 대시보드 정기 갱신’이 이 PC에서 평일 08:00·18:00 KST에 검증된 `python -m pipeline.refresh --publish`를 실행한다. 중복 Windows 작업이나 별도 런처를 만들지 않는다.
- 외부 수집·정적 빌드·Vercel 게시를 수동으로 심야에 실행하지 않는다. 자동 실행이 실패하면 직전 정상 사이트를 유지하고, 원인·실패 원장·필요한 인증만 보고한다.
- 일반 변경 후 게시 전에는 최소한 `python -m unittest discover -s tests`, `python scripts/validate.py`, `node scripts/test_charts.cjs`, `node scripts/test_dashboard.cjs`를 실행한다. 확장 모듈 변경이면 `python scripts/validate_extended.py`, `node scripts/test_extended.cjs`도 실행하고 `python scripts/write_status_docs.py`로 생성 문서를 갱신한다.

## 다음 행동 원칙

1. 새 실행의 원장·실패 원인·운영 사이트 상태를 먼저 대조한다. 같은 빈티지를 즉시 재수집하거나 실패를 정상값으로 바꾸지 않는다.
2. 공개 원자료가 필요한 범위(15개 재무, 신규 관계 가격, 13F, GDELT, 기업 뉴스·임상 관측)는 승인 자동 실행의 실제 결과로만 닫는다.
3. 원본 비공개 파라미터·제작자 비교, 유료 또는 인증 데이터, 팀 공유 DB·서버 인증은 사용자 선택·권한 없이는 구현하거나 대체하지 않는다.
4. 공개 저장소에는 코드·계약·압축된 계산 결과만 둔다. 원응답·비공개 보고서·자격 증명·원자료는 형제 `sangsangin-investment-data`에만 둔다.
5. 각 변경은 계약·테스트·공개 설명을 함께 갱신한다. 원본 스냅샷·제작자·소스 사이트 링크를 공개 페이지에 다시 넣지 않는다.

## 현재 목표 상태

Markdown 인계·계약·현황에 남은 범위를 전수 대조하는 목표는 외부 원자료와 사용자 권한이 필요한 항목 때문에 차단 상태로 기록됐다. 새 채팅은 위 자동 실행의 최신 결과 또는 사용자가 제공하는 권한·자료를 근거로 재개한다. 구현되지 않은 값이나 비공개 설정을 추정해 목표를 완료 처리하지 않는다.
