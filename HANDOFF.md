# 다음 작업 인수인계

현재 26개 가이드 중 RS·모멘텀을 실제 데이터 화면으로 전환했다. **두 탭의 완전 구현을 선언하지 않았다.** [구현 현황](research/IMPLEMENTATION_STATUS.md)과 [계산 계약](research/PRICE_CONTRACT.md)을 먼저 읽는다.

- 원본 보존본은 이 저장소의 형제 프로젝트 `aragorn_investium_study/`에 있다. 요청 명세·HTML/JS·JSON·141개 차트가 모두 로컬에 있으므로 원본 사이트를 다시 크롤링하지 않는다.
- 가격 원자료는 형제 `sangsangin-investment-data/2026-09-08/`에 있다. 압축 CSV와 manifest 총 약 1.9 MB, 61개 계열이다. 이 폴더는 Git 저장소 밖이다.
- 공개 `docs/data/rs.json`, `docs/data/momentum.json`은 약 0.96 MB의 독립 계산 결과다. 실제 수집 데이터를 썼으며 참조 이미지·정적 원본 수치·모의 시계열을 화면에 넣지 않았다.
- Python 3.13.5, requirements의 기존 설치 환경으로 실행. Node는 dependency 없는 구문/출력 검증에만 필요하다.
- 수집은 `python -m pipeline.collect --as-of YYYY-MM-DD`, 오프라인 계산은 `python -m pipeline.build --vintage YYYY-MM-DD`. 자동 일정은 없다. 누적 원자료 한도 32 MiB에 도달하면 먼저 사용자에게 물어본다.
- 공개 사이트는 GitHub Pages, main의 docs. Sites/Cloudflare로 옮기지 않는다. 사용자 요청에 따라 작은 코드·문서·파생 결과 공개가 허용되어 있다.
- 기준일 대조로 국내 IT(후보 266370), 대형/중소형(후보 069500/226980)은 일치하지 않아 제외했다. 이 둘을 다시 연결하려면 원본 실계열을 확인한다. 조선·지주·가치/성장·경기민감/방어도 미확인이다.
- 원본 US 17페어 스프레드는 같은 날짜 Yahoo Close로 17/17 표시 정밀도 내 일치. KR 연결 12페어는 Adj Close 기준 11/12 일치. z 차이는 남았다. 이 단일 시점으로 엔진 동일성을 주장하지 않는다.
- 모멘텀의 32자산은 모두 가격이 있는 2026-09-04로 통일했다. 섹터는 2026-09-08. 비교 날짜를 무시하고 원본 최신값과 단순 차이를 내지 않는다.
- 그룹 필터는 로컬 데이터만 재사용한다. 브라우저 UI/픽셀 테스트는 수행하지 않았고, 순수 계산·차트 구조·HTML 출력·공개 파일 검증을 수행했다.

다음 우선 작업은 RS 미완성 요소를 확정하고 ETF/멀티에셋 모듈을 추가하는 것이다. 매 작업 후 현황과 차트 대응표를 수정한다.
