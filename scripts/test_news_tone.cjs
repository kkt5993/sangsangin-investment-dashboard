'use strict';const fs=require('node:fs'),vm=require('node:vm'),path=require('node:path'),assert=require('node:assert/strict');const ctx=vm.createContext({window:{},URL});vm.runInContext(fs.readFileSync(path.join(__dirname,'../docs/company-news.js'),'utf8'),ctx);const C=ctx.window.CompanyNews;
const tone={available:true,label:'positive',score:.5,probabilities:{positive:.7,negative:.2,neutral:.1},tokens:10,analyzed_at:'2026-09-11T04:00:00+00:00'};let html=C.tone({tone});for(const text of ['긍정','70.0%','20.0%','10.0%','0.500'])assert(html.includes(text));assert(C.tone({tone:{available:false,reason:'<script>bad</script>'}}).includes('&lt;script&gt;'));assert(!C.tone({tone:{available:false,reason:'none'}}).includes('0.000'));assert(C.toneSummary({}).includes('미산출'));
const t={available:true,classified:2,total:3,complete:false,score:.25,labels:{positive:1,negative:0,neutral:1},revision:'abc123',analyzed_at:'2026-09-11',positive_hit:false};html=C.toneSummary({tone_summary:t});assert(html.includes('2/3'));assert(html.includes('일부 미분류'));assert(html.includes('기업의 호재·악재나 주가 상승확률이 아닙니다'));assert(html.includes('조건 미충족'));
console.log('PASS headline tone: probability labels, unknowns, partial coverage, interpretation and escaping');
